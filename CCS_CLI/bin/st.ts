#!/usr/bin/env -S npx tsx
import '../src/index.ts'; // In development we use tsx so this works
// For production build this would need to point to dist/index.js
// But for now let's assume we run via `tsx` or build output structure
